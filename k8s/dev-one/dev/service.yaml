apiVersion: v1
kind: Service
metadata:
  name: py--frontend
  namespace: dev-one
  labels:
    app.kubernetes.io/name: py--frontend
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: py--frontend
  ports:
    - name: http
      port: 80
      targetPort: 8080
